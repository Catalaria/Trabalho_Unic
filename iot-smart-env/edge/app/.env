# MQTT (use localhost se o broker está aí; ou "disabled" p/ desligar o worker)
MQTT_HOST=localhost
MQTT_PORT=1883
MQTT_TOPIC=iot/+/+/reading
MQTT_CLIENT_ID=edge-consumer

# DB
DB_URL=sqlite:///./edge_readings.db

# CORS (para o Vite do dashboard)
ALLOW_ORIGINS=http://localhost:5173

# token “admin” do demo
ADMIN_TOKEN=admin-demo-token
